#include "main.h"
/**
 * puts2 - main entry point
 * @str: variable in use
 * Description: prints every other character in a string
 */
void puts2(char *str)
{
	int i;

	for (i = 0; i < strlen(str); 
}
