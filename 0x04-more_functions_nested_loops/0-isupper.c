#include "main.h"
/**
 * _isupper - entry point
 * @c: variable to be checked
 * Description: checks for uppercaase character
 * Return: 1 if uppercase, 0 otherwise
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	return (0);
}
