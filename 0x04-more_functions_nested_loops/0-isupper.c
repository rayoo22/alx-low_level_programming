#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */

int _isupper(int c)
{
	/**
	 * to get the upper case
	 *
	 * and lower case
	 */
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	return (0);
}
