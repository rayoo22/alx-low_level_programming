#include "main.h"
/**
 * _strlen_recursion - returns the length of a string
 * @s: string to test its length
 * Return: the length of a string
 */
int _strlen_recursion(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
