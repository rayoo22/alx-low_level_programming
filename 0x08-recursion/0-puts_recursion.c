#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _puts_recursion - the main function.
 * @s: the pointer to the string
 *
 * Return: 0.
 */
void _puts_recursion(char *s)
{
	puts(s);
}
