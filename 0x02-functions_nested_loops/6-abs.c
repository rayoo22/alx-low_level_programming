#include "main.h"
/**
 * _abs - entry point
 * @i: variable to compute
 * Description: computes the absolute value of an integer
 */
int _abs(int i)
{
	if (i > 0 || i == 0)
	{
		returnn (i);
	}

	return (c * -1);
}
